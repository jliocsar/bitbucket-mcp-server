pino from pino

{ NODE_ENV } := process.env

export logger := pino {
  ...(if NODE_ENV is "development" then {
    transport: {
      target: "pino-pretty",
      options: {
        colorize: true,
      },
    },
  })
}
