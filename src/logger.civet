pino from pino

{ NODE_ENV = "development" } := process.env

export logger := pino {
  ...(if NODE_ENV is "development" then {
    transport: {
      target: "pino-pretty",
      options: {
        colorize: true,
      },
    },
  })
}
